// This is the configuration file for PostCSS, a tool for transforming CSS with JavaScript plugins.

export default {
  plugins: {
    // Adding the Tailwind CSS plugin to PostCSS.
    // This plugin processes your CSS and adds Tailwind CSS utilities, components, and base styles.
    tailwindcss: {},
    
    // Adding the Autoprefixer plugin to PostCSS.
    // This plugin automatically adds vendor prefixes to your CSS, ensuring cross-browser compatibility.
    autoprefixer: {},
  },
}


// PostCSS: A tool used to transform CSS with JavaScript plugins.
// tailwindcss plugin: Processes CSS and adds Tailwind CSS utilities, components, and base styles to your stylesheets.
// autoprefixer plugin: Automatically adds vendor prefixes to CSS rules, ensuring that your styles work across different browsers.