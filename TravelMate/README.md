# React + Vite

This template provides a minimal setup to get React working in Vite with HMR and some ESLint rules.

Currently, two official plugins are available:

- [@vitejs/plugin-react](https://github.com/vitejs/vite-plugin-react/blob/main/packages/plugin-react/README.md) uses [Babel](https://babeljs.io/) for Fast Refresh
- [@vitejs/plugin-react-swc](https://github.com/vitejs/vite-plugin-react-swc) uses [SWC](https://swc.rs/) for Fast Refresh


<!-- React + Vite: Indicates that the setup combines React, a popular JavaScript library for building user interfaces, with Vite, a fast build tool and development server.
Minimal Setup: The template offers a basic configuration to quickly get started with React and Vite.
Hot Module Replacement (HMR): A feature that allows modules to be updated in the browser without a full page reload, preserving the application's state.
ESLint Rules: Refers to integrating ESLint, a tool for identifying and fixing problems in JavaScript code, ensuring code quality and consistency.
Official Plugins:
@vitejs/plugin-react: Uses Babel, a widely-used JavaScript compiler, for enabling Fast Refresh in React applications.
@vitejs/plugin-react-swc: Uses SWC, a fast JavaScript and TypeScript compiler, as an alternative to Babel for Fast Refresh. -->